sudo: required
dist: precise

language: node_js
node_js:
  - "6"
cache:
  directories:
  - node_modules
install:
- npm install

script:
- npm run test
- npm run build

deploy:
  provider: heroku
  app: infinite-plains-24844

  strategy: api
  on:
    repo: Klammon/local_chat
